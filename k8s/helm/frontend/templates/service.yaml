apiVersion: v1
kind: Service
metadata:
  name: snisid-frontend
  labels:
    app: snisid-frontend
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: snisid-frontend
