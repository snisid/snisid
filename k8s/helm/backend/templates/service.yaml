apiVersion: v1
kind: Service
metadata:
  name: {{ include "snisid-backend.fullname" . }}
  labels:
    {{- include "snisid-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "snisid-backend.selectorLabels" . | nindent 4 }}
