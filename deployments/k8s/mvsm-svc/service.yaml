apiVersion: v1
kind: Service
metadata:
  name: mvsm-svc
  namespace: snisid-core
  labels:
    app: mvsm-svc
spec:
  type: ClusterIP
  ports:
  - port: 8127
    targetPort: 8127
    protocol: TCP
    name: http
  selector:
    app: mvsm-svc