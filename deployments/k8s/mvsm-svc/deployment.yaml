apiVersion: apps/v1
kind: Deployment
metadata:
  name: mvsm-svc
  namespace: snisid-core
  labels:
    app: mvsm-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mvsm-svc
  template:
    metadata:
      labels:
        app: mvsm-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8127"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: mvsm-svc
        image: snisid/mvsm-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8127
        env:
        - name: PORT
          value: "8127"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8127
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8127
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"