apiVersion: apps/v1
kind: Deployment
metadata:
  name: sipci-svc
  namespace: snisid-core
  labels:
    app: sipci-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sipci-svc
  template:
    metadata:
      labels:
        app: sipci-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8131"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: sipci-svc
        image: snisid/sipci-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8131
        env:
        - name: PORT
          value: "8131"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8131
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8131
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"