apiVersion: apps/v1
kind: Deployment
metadata:
  name: reso-svc
  namespace: snisid-core
  labels:
    app: reso-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: reso-svc
  template:
    metadata:
      labels:
        app: reso-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8101"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: reso-svc
        image: snisid/reso-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8101
        env:
        - name: PORT
          value: "8101"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8101
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8101
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"