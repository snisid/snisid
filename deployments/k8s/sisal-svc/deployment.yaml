apiVersion: apps/v1
kind: Deployment
metadata:
  name: sisal-svc
  namespace: snisid-core
  labels:
    app: sisal-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sisal-svc
  template:
    metadata:
      labels:
        app: sisal-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8128"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: sisal-svc
        image: snisid/sisal-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8128
        env:
        - name: PORT
          value: "8128"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8128
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8128
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"