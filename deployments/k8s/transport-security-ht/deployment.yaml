apiVersion: apps/v1
kind: Deployment
metadata:
  name: transport-security-ht
  namespace: snisid-national-security
  labels:
    app: transport-security-ht
spec:
  replicas: 2
  selector:
    matchLabels:
      app: transport-security-ht
  template:
    metadata:
      labels:
        app: transport-security-ht
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8307"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: transport-security-ht
        image: snisid/transport-security-ht:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8307
        env:
        - name: PORT
          value: "8307"
        - name: ENV
          value: "prod"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: snisid-secrets
              key: jwt-secret
        - name: KAFKA_BROKER
          value: "kafka:9092"
        - name: TRANSPORT_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: snisid_transport-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8307
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8307
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
