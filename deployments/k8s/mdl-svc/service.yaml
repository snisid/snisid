apiVersion: v1
kind: Service
metadata:
  name: mdl-svc
  namespace: snisid-core
  labels:
    app: mdl-svc
spec:
  type: ClusterIP
  ports:
  - port: 8094
    targetPort: 8094
    protocol: TCP
    name: http
  selector:
    app: mdl-svc
