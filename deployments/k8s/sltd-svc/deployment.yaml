apiVersion: apps/v1
kind: Deployment
metadata:
  name: sltd-svc
  namespace: snisid-core
  labels:
    app: sltd-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sltd-svc
  template:
    metadata:
      labels:
        app: sltd-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8108"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: sltd-svc
        image: snisid/sltd-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8108
        env:
        - name: PORT
          value: "8108"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8108
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8108
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"