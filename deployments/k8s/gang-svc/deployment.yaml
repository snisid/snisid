apiVersion: apps/v1
kind: Deployment
metadata:
  name: gang-svc
  namespace: snisid-core
  labels:
    app: gang-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gang-svc
  template:
    metadata:
      labels:
        app: gang-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8095"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: gang-svc
        image: snisid/gang-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8095
        env:
        - name: PORT
          value: "8095"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8095
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8095
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"