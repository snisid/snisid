apiVersion: apps/v1
kind: Deployment
metadata:
  name: sifr-svc
  namespace: snisid-core
  labels:
    app: sifr-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sifr-svc
  template:
    metadata:
      labels:
        app: sifr-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8106"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: sifr-svc
        image: snisid/sifr-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8106
        env:
        - name: PORT
          value: "8106"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8106
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8106
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"