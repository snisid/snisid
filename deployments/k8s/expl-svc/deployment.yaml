apiVersion: apps/v1
kind: Deployment
metadata:
  name: expl-svc
  namespace: snisid-core
  labels:
    app: expl-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: expl-svc
  template:
    metadata:
      labels:
        app: expl-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8104"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: expl-svc
        image: snisid/expl-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8104
        env:
        - name: PORT
          value: "8104"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8104
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8104
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"