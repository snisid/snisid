apiVersion: v1
kind: Service
metadata:
  name: radiation-safety-svc
  namespace: snisid-national-security
  labels:
    app: radiation-safety-svc
spec:
  type: ClusterIP
  ports:
  - port: 8308
    targetPort: 8308
    protocol: TCP
    name: http
  selector:
    app: radiation-safety-svc
