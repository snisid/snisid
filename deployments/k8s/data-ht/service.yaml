apiVersion: v1
kind: Service
metadata:
  name: data-ht
  namespace: snisid-core
  labels:
    app: data-ht
spec:
  type: ClusterIP
  ports:
  - port: 8213
    targetPort: 8213
    protocol: TCP
    name: http
  selector:
    app: data-ht
