apiVersion: apps/v1
kind: Deployment
metadata:
  name: hsm-svc
  namespace: snisid-core
  labels:
    app: hsm-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hsm-svc
  template:
    metadata:
      labels:
        app: hsm-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8090"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: hsm-svc
        image: snisid/hsm-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8090
        env:
        - name: HSM_DB_HOST
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: cockroach-host
        - name: HSM_DB_PORT
          value: "26257"
        - name: HSM_DB_NAME
          value: "snisid_hsm"
        - name: HSM_DB_USER
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: cockroach-user
        - name: HSM_DB_SSLMODE
          value: "disable"
        - name: HSM_KAFKA_BROKERS
          value: "kafka:9092"
        - name: HSM_SERVICE_PORT
          value: "8090"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8090
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8090
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
