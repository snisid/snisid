apiVersion: v1
kind: Service
metadata:
  name: hsm-svc
  namespace: snisid-core
  labels:
    app: hsm-svc
spec:
  type: ClusterIP
  ports:
  - port: 8090
    targetPort: 8090
    protocol: TCP
    name: http
  selector:
    app: hsm-svc
