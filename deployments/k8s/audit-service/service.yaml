apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: snisid-core
  labels:
    app: audit-service
spec:
  type: ClusterIP
  ports:
  - port: 8085
    targetPort: 8085
    protocol: TCP
    name: http
  selector:
    app: audit-service
