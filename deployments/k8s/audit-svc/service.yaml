apiVersion: v1
kind: Service
metadata:
  name: audit-svc
  namespace: snisid-core
  labels:
    app: audit-svc
spec:
  type: ClusterIP
  ports:
  - port: 8103
    targetPort: 8103
    protocol: TCP
    name: http
  selector:
    app: audit-svc
