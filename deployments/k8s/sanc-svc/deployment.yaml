apiVersion: apps/v1
kind: Deployment
metadata:
  name: sanc-svc
  namespace: snisid-core
  labels:
    app: sanc-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sanc-svc
  template:
    metadata:
      labels:
        app: sanc-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8100"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: sanc-svc
        image: snisid/sanc-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8100
        env:
        - name: PORT
          value: "8100"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8100
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8100
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"