apiVersion: v1
kind: Service
metadata:
  name: sanc-svc
  namespace: snisid-core
  labels:
    app: sanc-svc
spec:
  type: ClusterIP
  ports:
  - port: 8100
    targetPort: 8100
    protocol: TCP
    name: http
  selector:
    app: sanc-svc