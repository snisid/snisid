apiVersion: apps/v1
kind: Deployment
metadata:
  name: executive-protection-ht
  namespace: snisid-national-security
  labels:
    app: executive-protection-ht
spec:
  replicas: 2
  selector:
    matchLabels:
      app: executive-protection-ht
  template:
    metadata:
      labels:
        app: executive-protection-ht
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8306"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: executive-protection-ht
        image: snisid/executive-protection-ht:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8306
        env:
        - name: PORT
          value: "8306"
        - name: ENV
          value: "prod"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: snisid-secrets
              key: jwt-secret
        - name: KAFKA_BROKER
          value: "kafka:9092"
        - name: EXECPROT_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: snisid_execprot-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8306
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8306
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
