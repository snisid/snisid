apiVersion: v1
kind: Service
metadata:
  name: opr-svc
  namespace: snisid-core
  labels:
    app: opr-svc
spec:
  type: ClusterIP
  ports:
  - port: 8096
    targetPort: 8096
    protocol: TCP
    name: http
  selector:
    app: opr-svc