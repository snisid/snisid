apiVersion: apps/v1
kind: Deployment
metadata:
  name: opr-svc
  namespace: snisid-core
  labels:
    app: opr-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: opr-svc
  template:
    metadata:
      labels:
        app: opr-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8096"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: opr-svc
        image: snisid/opr-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8096
        env:
        - name: PORT
          value: "8096"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8096
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8096
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"