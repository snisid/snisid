apiVersion: v1
kind: Service
metadata:
  name: biar-svc
  namespace: snisid-core
  labels:
    app: biar-svc
spec:
  type: ClusterIP
  ports:
  - port: 8103
    targetPort: 8103
    protocol: TCP
    name: http
  selector:
    app: biar-svc