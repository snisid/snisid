apiVersion: apps/v1
kind: Deployment
metadata:
  name: biar-svc
  namespace: snisid-core
  labels:
    app: biar-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: biar-svc
  template:
    metadata:
      labels:
        app: biar-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8103"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: biar-svc
        image: snisid/biar-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8103
        env:
        - name: PORT
          value: "8103"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8103
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8103
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"