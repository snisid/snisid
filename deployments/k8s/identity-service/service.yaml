apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: snisid-core
  labels:
    app: identity-service
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: identity-service
