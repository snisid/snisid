apiVersion: apps/v1
kind: Deployment
metadata:
  name: sipep-svc
  namespace: snisid-core
  labels:
    app: sipep-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sipep-svc
  template:
    metadata:
      labels:
        app: sipep-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8092"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: sipep-svc
        image: snisid/sipep-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8092
        env:
        - name: PORT
          value: "8092"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8092
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8092
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"