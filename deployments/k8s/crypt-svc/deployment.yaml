apiVersion: apps/v1
kind: Deployment
metadata:
  name: crypt-svc
  namespace: snisid-core
  labels:
    app: crypt-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: crypt-svc
  template:
    metadata:
      labels:
        app: crypt-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8117"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: crypt-svc
        image: snisid/crypt-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8117
        env:
        - name: PORT
          value: "8117"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8117
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8117
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"