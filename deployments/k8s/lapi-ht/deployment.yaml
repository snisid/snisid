apiVersion: apps/v1
kind: Deployment
metadata:
  name: lapi-ht
  namespace: snisid-core
  labels:
    app: lapi-ht
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lapi-ht
  template:
    metadata:
      labels:
        app: lapi-ht
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8216"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: lapi-ht
        image: snisid/lapi-ht:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8216
        env:
        - name: LAPI_DB_HOST
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: cockroach-host
        - name: LAPI_DB_PORT
          value: "26257"
        - name: LAPI_DB_NAME
          value: "snisid_lapi"
        - name: LAPI_DB_USER
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: cockroach-user
        - name: LAPI_DB_SSLMODE
          value: "disable"
        - name: LAPI_KAFKA_BROKERS
          value: "kafka:9092"
        - name: LAPI_SERVICE_PORT
          value: "8216"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8216
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8216
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
