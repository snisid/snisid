apiVersion: apps/v1
kind: Deployment
metadata:
  name: air-defense-ht
  namespace: snisid-national-security
  labels:
    app: air-defense-ht
spec:
  replicas: 2
  selector:
    matchLabels:
      app: air-defense-ht
  template:
    metadata:
      labels:
        app: air-defense-ht
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8303"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: air-defense-ht
        image: snisid/air-defense-ht:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8303
        env:
        - name: PORT
          value: "8303"
        - name: ENV
          value: "prod"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: snisid-secrets
              key: jwt-secret
        - name: KAFKA_BROKER
          value: "kafka:9092"
        - name: AIRDEF_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: snisid_airdefense-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8303
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8303
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
