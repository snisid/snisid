apiVersion: apps/v1
kind: Deployment
metadata:
  name: rdep-svc
  namespace: snisid-core
  labels:
    app: rdep-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rdep-svc
  template:
    metadata:
      labels:
        app: rdep-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8094"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: rdep-svc
        image: snisid/rdep-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8094
        env:
        - name: PORT
          value: "8094"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8094
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8094
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"