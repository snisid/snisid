apiVersion: v1
kind: Service
metadata:
  name: rdep-svc
  namespace: snisid-core
  labels:
    app: rdep-svc
spec:
  type: ClusterIP
  ports:
  - port: 8094
    targetPort: 8094
    protocol: TCP
    name: http
  selector:
    app: rdep-svc