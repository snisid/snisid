apiVersion: apps/v1
kind: Deployment
metadata:
  name: fips-cert-svc
  namespace: snisid-core
  labels:
    app: fips-cert-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fips-cert-svc
  template:
    metadata:
      labels:
        app: fips-cert-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8098"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: fips-cert-svc
        image: snisid/fips-cert-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8098
        env:
        - name: FIPS_DB_HOST
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: cockroach-host
        - name: FIPS_DB_PORT
          value: "26257"
        - name: FIPS_DB_NAME
          value: "snisid_fips"
        - name: FIPS_DB_USER
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: cockroach-user
        - name: FIPS_DB_SSLMODE
          value: "disable"
        - name: FIPS_KAFKA_BROKERS
          value: "kafka:9092"
        - name: FIPS_SERVICE_PORT
          value: "8098"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8098
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8098
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
