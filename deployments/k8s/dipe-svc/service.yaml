apiVersion: v1
kind: Service
metadata:
  name: dipe-svc
  namespace: snisid-core
  labels:
    app: dipe-svc
spec:
  type: ClusterIP
  ports:
  - port: 8118
    targetPort: 8118
    protocol: TCP
    name: http
  selector:
    app: dipe-svc