apiVersion: apps/v1
kind: Deployment
metadata:
  name: counterintel-ht
  namespace: snisid-national-security
  labels:
    app: counterintel-ht
spec:
  replicas: 2
  selector:
    matchLabels:
      app: counterintel-ht
  template:
    metadata:
      labels:
        app: counterintel-ht
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8310"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: counterintel-ht
        image: snisid/counterintel-ht:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8310
        env:
        - name: PORT
          value: "8310"
        - name: ENV
          value: "prod"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: snisid-secrets
              key: jwt-secret
        - name: KAFKA_BROKER
          value: "kafka:9092"
        - name: COUNTERINTEL_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: snisid_counterintel-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8310
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8310
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
