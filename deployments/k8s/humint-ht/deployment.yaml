apiVersion: apps/v1
kind: Deployment
metadata:
  name: humint-ht
  namespace: snisid-national-security
  labels:
    app: humint-ht
spec:
  replicas: 2
  selector:
    matchLabels:
      app: humint-ht
  template:
    metadata:
      labels:
        app: humint-ht
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8302"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: humint-ht
        image: snisid/humint-ht:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8302
        env:
        - name: PORT
          value: "8302"
        - name: ENV
          value: "prod"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: snisid-secrets
              key: jwt-secret
        - name: KAFKA_BROKER
          value: "kafka:9092"
        - name: HUMINT_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: snisid_humint-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8302
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8302
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
