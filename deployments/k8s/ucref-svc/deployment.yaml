apiVersion: apps/v1
kind: Deployment
metadata:
  name: ucref-svc
  namespace: snisid-core
  labels:
    app: ucref-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ucref-svc
  template:
    metadata:
      labels:
        app: ucref-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8112"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: ucref-svc
        image: snisid/ucref-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8112
        env:
        - name: PORT
          value: "8112"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8112
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8112
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"