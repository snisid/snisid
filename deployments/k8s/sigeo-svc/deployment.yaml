apiVersion: apps/v1
kind: Deployment
metadata:
  name: sigeo-svc
  namespace: snisid-core
  labels:
    app: sigeo-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sigeo-svc
  template:
    metadata:
      labels:
        app: sigeo-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8125"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: sigeo-svc
        image: snisid/sigeo-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8125
        env:
        - name: PORT
          value: "8125"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8125
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8125
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"