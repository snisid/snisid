apiVersion: v1
kind: Service
metadata:
  name: mil-c2-ht
  namespace: snisid-national-security
  labels:
    app: mil-c2-ht
spec:
  type: ClusterIP
  ports:
  - port: 8304
    targetPort: 8304
    protocol: TCP
    name: http
  selector:
    app: mil-c2-ht
