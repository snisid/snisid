apiVersion: apps/v1
kind: Deployment
metadata:
  name: corr-svc
  namespace: snisid-core
  labels:
    app: corr-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: corr-svc
  template:
    metadata:
      labels:
        app: corr-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8130"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: corr-svc
        image: snisid/corr-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8130
        env:
        - name: PORT
          value: "8130"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8130
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8130
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"