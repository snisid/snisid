apiVersion: v1
kind: Service
metadata:
  name: corr-svc
  namespace: snisid-core
  labels:
    app: corr-svc
spec:
  type: ClusterIP
  ports:
  - port: 8130
    targetPort: 8130
    protocol: TCP
    name: http
  selector:
    app: corr-svc