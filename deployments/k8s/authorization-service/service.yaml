apiVersion: v1
kind: Service
metadata:
  name: authorization-service
  namespace: snisid-core
  labels:
    app: authorization-service
spec:
  type: ClusterIP
  ports:
  - port: 8084
    targetPort: 8084
    protocol: TCP
    name: http
  selector:
    app: authorization-service
