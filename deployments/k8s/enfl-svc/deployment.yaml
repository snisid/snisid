apiVersion: apps/v1
kind: Deployment
metadata:
  name: enfl-svc
  namespace: snisid-core
  labels:
    app: enfl-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: enfl-svc
  template:
    metadata:
      labels:
        app: enfl-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8119"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: enfl-svc
        image: snisid/enfl-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8119
        env:
        - name: PORT
          value: "8119"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8119
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8119
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"