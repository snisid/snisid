apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-verify-svc
  namespace: snisid-core
  labels:
    app: employee-verify-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: employee-verify-svc
  template:
    metadata:
      labels:
        app: employee-verify-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8096"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: employee-verify-svc
        image: snisid/employee-verify-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8096
        env:
        - name: EMPVERIFY_DB_HOST
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: cockroach-host
        - name: EMPVERIFY_DB_PORT
          value: "26257"
        - name: EMPVERIFY_DB_NAME
          value: "snisid_empverify"
        - name: EMPVERIFY_DB_USER
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: cockroach-user
        - name: EMPVERIFY_DB_SSLMODE
          value: "disable"
        - name: EMPVERIFY_KAFKA_BROKERS
          value: "kafka:9092"
        - name: EMPVERIFY_SERVICE_PORT
          value: "8096"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8096
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8096
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
