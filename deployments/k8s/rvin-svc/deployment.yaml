apiVersion: apps/v1
kind: Deployment
metadata:
  name: rvin-svc
  namespace: snisid-core
  labels:
    app: rvin-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rvin-svc
  template:
    metadata:
      labels:
        app: rvin-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8120"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: rvin-svc
        image: snisid/rvin-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8120
        env:
        - name: PORT
          value: "8120"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8120
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8120
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"