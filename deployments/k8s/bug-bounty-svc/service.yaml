apiVersion: v1
kind: Service
metadata:
  name: bug-bounty-svc
  namespace: snisid-core
  labels:
    app: bug-bounty-svc
spec:
  type: ClusterIP
  ports:
  - port: 8100
    targetPort: 8100
    protocol: TCP
    name: http
  selector:
    app: bug-bounty-svc
