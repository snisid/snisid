apiVersion: v1
kind: Service
metadata:
  name: chef-svc
  namespace: snisid-core
  labels:
    app: chef-svc
spec:
  type: ClusterIP
  ports:
  - port: 8097
    targetPort: 8097
    protocol: TCP
    name: http
  selector:
    app: chef-svc