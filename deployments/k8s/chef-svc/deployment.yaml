apiVersion: apps/v1
kind: Deployment
metadata:
  name: chef-svc
  namespace: snisid-core
  labels:
    app: chef-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chef-svc
  template:
    metadata:
      labels:
        app: chef-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8097"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: chef-svc
        image: snisid/chef-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8097
        env:
        - name: PORT
          value: "8097"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8097
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8097
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"