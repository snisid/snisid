apiVersion: apps/v1
kind: Deployment
metadata:
  name: sigdc-svc
  namespace: snisid-core
  labels:
    app: sigdc-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sigdc-svc
  template:
    metadata:
      labels:
        app: sigdc-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8126"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: sigdc-svc
        image: snisid/sigdc-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8126
        env:
        - name: PORT
          value: "8126"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8126
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8126
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"