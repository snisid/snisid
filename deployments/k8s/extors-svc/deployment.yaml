apiVersion: apps/v1
kind: Deployment
metadata:
  name: extors-svc
  namespace: snisid-core
  labels:
    app: extors-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: extors-svc
  template:
    metadata:
      labels:
        app: extors-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8116"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: extors-svc
        image: snisid/extors-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8116
        env:
        - name: PORT
          value: "8116"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8116
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8116
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"