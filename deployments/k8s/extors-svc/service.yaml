apiVersion: v1
kind: Service
metadata:
  name: extors-svc
  namespace: snisid-core
  labels:
    app: extors-svc
spec:
  type: ClusterIP
  ports:
  - port: 8116
    targetPort: 8116
    protocol: TCP
    name: http
  selector:
    app: extors-svc