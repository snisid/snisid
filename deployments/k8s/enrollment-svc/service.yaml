apiVersion: v1
kind: Service
metadata:
  name: enrollment-svc
  namespace: snisid-core
  labels:
    app: enrollment-svc
spec:
  type: ClusterIP
  ports:
  - port: 8091
    targetPort: 8091
    protocol: TCP
    name: http
  selector:
    app: enrollment-svc
