apiVersion: apps/v1
kind: Deployment
metadata:
  name: siar-svc
  namespace: snisid-core
  labels:
    app: siar-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: siar-svc
  template:
    metadata:
      labels:
        app: siar-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8102"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: siar-svc
        image: snisid/siar-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8102
        env:
        - name: PORT
          value: "8102"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8102
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8102
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"