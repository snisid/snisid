apiVersion: v1
kind: Service
metadata:
  name: siar-svc
  namespace: snisid-core
  labels:
    app: siar-svc
spec:
  type: ClusterIP
  ports:
  - port: 8102
    targetPort: 8102
    protocol: TCP
    name: http
  selector:
    app: siar-svc