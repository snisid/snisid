apiVersion: v1
kind: Service
metadata:
  name: accessibility-svc
  namespace: snisid-core
  labels:
    app: accessibility-svc
spec:
  type: ClusterIP
  ports:
  - port: 8101
    targetPort: 8101
    protocol: TCP
    name: http
  selector:
    app: accessibility-svc
