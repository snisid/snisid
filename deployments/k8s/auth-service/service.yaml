apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: snisid-core
  labels:
    app: auth-service
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
    name: http
  selector:
    app: auth-service
