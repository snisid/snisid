apiVersion: v1
kind: Service
metadata:
  name: pki-bridge-svc
  namespace: snisid-core
  labels:
    app: pki-bridge-svc
spec:
  type: ClusterIP
  ports:
  - port: 8099
    targetPort: 8099
    protocol: TCP
    name: http
  selector:
    app: pki-bridge-svc
