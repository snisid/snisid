apiVersion: v1
kind: Service
metadata:
  name: classification-mgmt-ht
  namespace: snisid-national-security
  labels:
    app: classification-mgmt-ht
spec:
  type: ClusterIP
  ports:
  - port: 8313
    targetPort: 8313
    protocol: TCP
    name: http
  selector:
    app: classification-mgmt-ht
