apiVersion: v1
kind: Service
metadata:
  name: foves-ht
  namespace: snisid-core
  labels:
    app: foves-ht
spec:
  type: ClusterIP
  ports:
  - port: 8215
    targetPort: 8215
    protocol: TCP
    name: http
  selector:
    app: foves-ht
