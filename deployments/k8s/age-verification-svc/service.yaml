apiVersion: v1
kind: Service
metadata:
  name: age-verification-svc
  namespace: snisid-core
  labels:
    app: age-verification-svc
spec:
  type: ClusterIP
  ports:
  - port: 8095
    targetPort: 8095
    protocol: TCP
    name: http
  selector:
    app: age-verification-svc
