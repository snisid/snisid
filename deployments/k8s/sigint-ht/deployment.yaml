apiVersion: apps/v1
kind: Deployment
metadata:
  name: sigint-ht
  namespace: snisid-national-security
  labels:
    app: sigint-ht
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sigint-ht
  template:
    metadata:
      labels:
        app: sigint-ht
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8301"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: sigint-ht
        image: snisid/sigint-ht:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8301
        env:
        - name: PORT
          value: "8301"
        - name: ENV
          value: "prod"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: snisid-secrets
              key: jwt-secret
        - name: KAFKA_BROKER
          value: "kafka:9092"
        - name: SIGINT_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: snisid_sigint-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8301
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8301
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
