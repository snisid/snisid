apiVersion: apps/v1
kind: Deployment
metadata:
  name: fir-svc
  namespace: snisid-core
  labels:
    app: fir-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fir-svc
  template:
    metadata:
      labels:
        app: fir-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8093"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: fir-svc
        image: snisid/fir-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8093
        env:
        - name: PORT
          value: "8093"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8093
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8093
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"