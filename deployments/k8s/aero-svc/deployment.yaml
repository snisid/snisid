apiVersion: apps/v1
kind: Deployment
metadata:
  name: aero-svc
  namespace: snisid-core
  labels:
    app: aero-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aero-svc
  template:
    metadata:
      labels:
        app: aero-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8109"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: aero-svc
        image: snisid/aero-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8109
        env:
        - name: PORT
          value: "8109"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8109
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8109
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"