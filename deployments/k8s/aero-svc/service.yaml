apiVersion: v1
kind: Service
metadata:
  name: aero-svc
  namespace: snisid-core
  labels:
    app: aero-svc
spec:
  type: ClusterIP
  ports:
  - port: 8109
    targetPort: 8109
    protocol: TCP
    name: http
  selector:
    app: aero-svc