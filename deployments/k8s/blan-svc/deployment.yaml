apiVersion: apps/v1
kind: Deployment
metadata:
  name: blan-svc
  namespace: snisid-core
  labels:
    app: blan-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: blan-svc
  template:
    metadata:
      labels:
        app: blan-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8115"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: blan-svc
        image: snisid/blan-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8115
        env:
        - name: PORT
          value: "8115"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8115
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8115
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"