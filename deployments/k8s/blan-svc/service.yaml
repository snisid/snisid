apiVersion: v1
kind: Service
metadata:
  name: blan-svc
  namespace: snisid-core
  labels:
    app: blan-svc
spec:
  type: ClusterIP
  ports:
  - port: 8115
    targetPort: 8115
    protocol: TCP
    name: http
  selector:
    app: blan-svc