apiVersion: v1
kind: Service
metadata:
  name: certification-svc
  namespace: snisid-core
  labels:
    app: certification-svc
spec:
  type: ClusterIP
  ports:
  - port: 8093
    targetPort: 8093
    protocol: TCP
    name: http
  selector:
    app: certification-svc
