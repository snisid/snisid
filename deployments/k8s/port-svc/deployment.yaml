apiVersion: apps/v1
kind: Deployment
metadata:
  name: port-svc
  namespace: snisid-core
  labels:
    app: port-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: port-svc
  template:
    metadata:
      labels:
        app: port-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8111"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: port-svc
        image: snisid/port-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8111
        env:
        - name: PORT
          value: "8111"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8111
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8111
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"