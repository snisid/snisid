apiVersion: apps/v1
kind: Deployment
metadata:
  name: dpide-svc
  namespace: snisid-core
  labels:
    app: dpide-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dpide-svc
  template:
    metadata:
      labels:
        app: dpide-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8121"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: dpide-svc
        image: snisid/dpide-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8121
        env:
        - name: PORT
          value: "8121"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8121
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8121
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"