apiVersion: apps/v1
kind: Deployment
metadata:
  name: terr-svc
  namespace: snisid-core
  labels:
    app: terr-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: terr-svc
  template:
    metadata:
      labels:
        app: terr-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8098"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: terr-svc
        image: snisid/terr-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8098
        env:
        - name: PORT
          value: "8098"
        - name: ENV
          value: "prod"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: postgres-url
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8098
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8098
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"