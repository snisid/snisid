apiVersion: v1
kind: Service
metadata:
  name: bio-ht
  namespace: snisid-core
  labels:
    app: bio-ht
spec:
  type: ClusterIP
  ports:
  - port: 8203
    targetPort: 8203
    protocol: TCP
    name: http
  selector:
    app: bio-ht
