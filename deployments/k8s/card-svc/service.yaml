apiVersion: v1
kind: Service
metadata:
  name: card-svc
  namespace: snisid-core
  labels:
    app: card-svc
spec:
  type: ClusterIP
  ports:
  - port: 8092
    targetPort: 8092
    protocol: TCP
    name: http
  selector:
    app: card-svc
