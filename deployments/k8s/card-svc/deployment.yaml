apiVersion: apps/v1
kind: Deployment
metadata:
  name: card-svc
  namespace: snisid-core
  labels:
    app: card-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: card-svc
  template:
    metadata:
      labels:
        app: card-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8092"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: card-svc
        image: snisid/card-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8092
        env:
        - name: CARD_DB_HOST
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: cockroach-host
        - name: CARD_DB_PORT
          value: "26257"
        - name: CARD_DB_NAME
          value: "snisid_card_svc"
        - name: CARD_DB_USER
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: cockroach-user
        - name: CARD_DB_SSLMODE
          value: "disable"
        - name: CARD_KAFKA_BROKERS
          value: "kafka:9092"
        - name: CARD_SERVICE_PORT
          value: "8092"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8092
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8092
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
