apiVersion: apps/v1
kind: Deployment
metadata:
  name: dr-svc
  namespace: snisid-core
  labels:
    app: dr-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dr-svc
  template:
    metadata:
      labels:
        app: dr-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8097"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: dr-svc
        image: snisid/dr-svc:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8097
        env:
        - name: DR_DB_HOST
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: cockroach-host
        - name: DR_DB_PORT
          value: "26257"
        - name: DR_DB_NAME
          value: "snisid_dr"
        - name: DR_DB_USER
          valueFrom:
            secretKeyRef:
              name: snisid-db-credentials
              key: cockroach-user
        - name: DR_DB_SSLMODE
          value: "disable"
        - name: DR_KAFKA_BROKERS
          value: "kafka:9092"
        - name: DR_SERVICE_PORT
          value: "8097"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8097
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8097
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "1000m"
            memory: "512Mi"
